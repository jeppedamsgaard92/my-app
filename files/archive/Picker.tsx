{/* <Picker
          selectedValue={brewNote.rating.toString()}
          onValueChange={(value: string) => handleInputChange("rating", parseInt(value, 10))}
        >
          <Picker.Item label="How was that brew?" value="" />
          {[1, 2, 3, 4, 5].map((value) => (
            <Picker.Item key={value} label={`${value} Star${value > 1 ? "s" : ""}`} value={value.toString()} />
          ))}
        </Picker> */}